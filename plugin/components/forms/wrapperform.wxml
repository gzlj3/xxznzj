<view class="weui-cells weui-cells_after-title">  
  <block wx:for="{{fmMetas}}" wx:key="{{item.name}}">
    <!-- <block wx:if="{{item.type==='multi'}}">
      <view class="weui-cells__title">{{item.label}}{{morezh_index+1}}
        <view style='display:inline-block;width:70%;color:red;text-align:right' data-item="{{morezh_index}}" ontap="onDeleteMorezh">删除</view>
      </view>
    </block> -->
    <wrapperinput id="_wrapperinput" value="{{currentObject[item.name]}}" options="{{item}}" bindinput="onInputChange" bindchange="onInputChange"/>
  </block>
</view>
